---
const { title, backHref, backLabel = "← Back", className = "" } = Astro.props;
---
<section class:list={["section", className]}>
  {backHref && (
    <a class="back-link" href={backHref}>
      {backLabel}
    </a>
  )}
  <h1>{title}</h1>
  <slot />
</section>
