---
const { title, backHref, backLabel = "← Back" } = Astro.props;
---
<section class="section">
  {backHref && (
    <a class="back-link" href={backHref}>
      {backLabel}
    </a>
  )}
  <h1>{title}</h1>
  <slot />
</section>
