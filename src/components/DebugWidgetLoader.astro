---
import loaderUrl from "../scripts/debugWidgetLoader.ts?url";

type Props = {
  pageKey?: string;
};

const { pageKey } = Astro.props;
---
<script
  type="module"
  src={loaderUrl}
  data-page-key={pageKey}
></script>
