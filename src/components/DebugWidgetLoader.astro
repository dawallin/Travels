---
import loaderUrl from "../scripts/debugWidgetLoader.js?url";

type Props = {
  pageKey?: string;
};

const { pageKey } = Astro.props;
---
<script
  type="module"
  src={loaderUrl}
  data-page-key={pageKey}
></script>
